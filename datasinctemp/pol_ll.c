#include <stdio.h>
#include <stdlib.h>
struct Node
{
    int coeff;
    int pow;
    struct Node *next;
};

void insert(int coeff, int data)
{
}

int main()
{
    int n=0;
    while (n != -1)
    {
        printf("Enter two numbers: coeff and power");
        int a,b;
        scanf("%d %d",&a,&b);
    }
}